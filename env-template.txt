# Copiar este contenido y crear .env con tus valores reales

# Server Configuration
NODE_ENV=development
PORT=3001
API_PREFIX=api/v1

# Database
DATABASE_URL=postgresql://contextai_user:dev_password@localhost:5432/contextai
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=contextai_user
DATABASE_PASSWORD=dev_password
DATABASE_NAME=contextai

# Auth0
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_AUDIENCE=https://api.contextai.com
AUTH0_ISSUER=https://your-tenant.auth0.com/

# AI Services - Google Genkit
GOOGLE_API_KEY=your-gemini-api-key-here
GENKIT_ENV=dev

# Pinecone Vector Database
# Use different index names (or separate API keys) per environment:
#   Dev:  PINECONE_INDEX=context-ai-dev   (local / staging)
#   Prod: PINECONE_INDEX=context-ai-prod  (production)
# Both indexes must be created in the Pinecone dashboard with
# dimensions=3072 (gemini-embedding-001), metric=cosine, type=serverless.
PINECONE_API_KEY=pcsk_xxxxx
PINECONE_INDEX=context-ai-dev

# CORS & Security
FRONTEND_URL=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Observability
SENTRY_DSN=
LOG_LEVEL=debug
